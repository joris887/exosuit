# Workflow enforcement rules (Ralph Loop pattern)
# Controls how the Stop hook detects and blocks incomplete workflows.

# Maximum times the Stop hook will block before allowing exit unconditionally.
# Prevents infinite loops if a workflow gets stuck.
max_iterations: 5

# Failure state older than this many hours is treated as stale (abandoned).
# Stale workflows don't block exit.
stale_hours: 4
