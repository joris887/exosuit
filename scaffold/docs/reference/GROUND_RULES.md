# Ground Rules
<!-- Generated by /bootstrap (A3.5b) | Last reviewed: YYYY-MM-DD -->
<!-- Target: 3-7 rules. More than 7? Some are probably guidance, not ground rules. -->

## Architecture Summary
<!-- 2-3 sentences: architectural pattern, key technology choices, primary quality attributes.
     This context helps AI interpret rules correctly at edge cases. -->

## Rules
<!-- Each rule uses RFC 2119: MUST = non-negotiable (violations halt), SHOULD = strong preference (exceptions require justification). -->

<!-- EXAMPLE (delete when populating):
### GR-001: Domain layer independence
- **Level**: MUST
- **Category**: dependencies
- **Statement**: Domain modules MUST NOT import from infrastructure, framework, or delivery layers.
- **Rationale**: Couples business logic to delivery mechanisms, making the domain untestable and non-portable.
- **Enforced-by**: ai: "Never add infrastructure/framework imports into domain files" | review: check imports in domain/
- **Exceptions**: None — define a port interface instead.
- **ADR**: ADR-0007 (if this rule was promoted from an ADR — provides full decision context and rejected alternatives)
-->

<!-- BOOTSTRAP DEFAULTS — suggest based on detected architecture:

REST API / Backend service:
  - Inward dependency direction (domain has zero outward deps)
  - No business logic in route handlers (controller delegates to service layer)
  - Schema-validated inputs (all API inputs validated against schema before processing)

React SPA / Frontend:
  - Single state management approach (one tool for server state, one for client state)
  - No direct API calls in components (use data-fetching layer)
  - Component boundary enforcement (pages compose features, features compose UI primitives)

CLI tool / Library:
  - Public API stability (breaking changes require major version bump)
  - No side effects in core logic (I/O at boundaries only)
  - Dependency budget (max N direct dependencies)

Microservices:
  - Services communicate only through defined contracts (no shared databases)
  - Each service owns its datastore exclusively
  - Events for cross-boundary state changes (no synchronous cross-service mutations)

Monolith:
  - Module boundaries enforced (no cross-module direct database access)
  - Shared kernel explicitly defined (common code in designated shared/ directory)
  - Dependency direction follows defined layer order

Universal (suggest for any project):
  - No hardcoded secrets (credentials from env vars or secrets manager)
  - Test coverage floor (coverage MUST NOT decrease sprint-over-sprint)
  - No silent failures (errors MUST be logged or surfaced, never swallowed)

AI-Assisted Development (suggest when AI tools are used):
  - All AI-suggested dependencies MUST be verified to exist in their package registry before adding
  - AI-generated values for secrets, tokens, and keys MUST NOT be used — always generate fresh
  - Authentication and cryptographic code SHOULD use established libraries, not AI-generated implementations
  - All AI-generated code SHOULD be scanned for the CWE top 15 (see .claude/rules/security.md)

CATEGORIES: dependencies | boundaries | data-flow | security | technology | operational | ai-development
-->

## Exception Log
<!-- Approved deviations from ground rules. Include expiry to prevent permanent exceptions. -->
| Rule | Justification | Scope | Expires | Approved by |
|------|---------------|-------|---------|-------------|

## Change History
<!-- When rules change, record why. Reference ADR if applicable. -->
| Date | Rule | Change | Rationale |
|------|------|--------|-----------|
